# Scratch files of the scripts that run hundreds of times (they delete these themselves
# when they finish normally)
nm/boot.R76/
nm/rpt.R76/
nm/sse.R76/
nm/_*.ctl
data/_*.csv
# nmw's report PDFs are remade by R/build.R
nm/**/*.pdf
nm/**/*.Rout
# The English edition's build writes any figure a snippet draws to a scratch folder
En/figures-check/
