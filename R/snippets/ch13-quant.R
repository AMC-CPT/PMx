# 순열 검정은 카이제곱을 가정하지 않는다. 그래서 거꾸로, 카이제곱 근사가
# 맞는지를 순열로 시험할 수 있다. 분위수마다 나란히 놓고 본다.
pr <- c(0.5, 0.8, 0.9, 0.95, 0.99)
round(rbind(순열 = quantile(null, pr), 카이제곱 = qchisq(pr, 2)), 2)

# 그런데 꼬리는 다르다.
c(귀무최대 = round(max(null), 2), `10 초과` = sum(null > 10),
  `20 초과` = sum(null > 20))
